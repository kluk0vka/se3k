replicaCount: 2
image:
  repository: registry.local:5000/staybook/booking-service
  tag: "0.1.0"
  pullPolicy: IfNotPresent
service:
  port: 8000
resources:
  requests: {cpu: 50m, memory: 96Mi}
  limits: {memory: 256Mi}
autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 6
  targetCPUUtilizationPercentage: 70
kafka:
  bootstrapServers: kafka-kafka-bootstrap.kafka:9092
env:
  POSTGRES_HOST: postgres.data
secretEnvFrom:
  - booking-service-postgres
